# 24.09.2026.

## Duge crte i u statičkom tekstu AI forme

Ranije je sređeno samo ono što AI napiše. Ostale su duge crte u tekstu koji je
hardkodovan u ekranu, pa su se i dalje videle: podnaslov prvog ekrana, linija o
hitnim slučajevima, skala krhkosti i njeni krajevi, poruke o greškama.

Izmenjeno:

- `src/screens/ImmersiveConversation.jsx` - podnaslov („kako god vam je lakše"),
  hitna linija (sada u zagradi), „Klinička skala krhkosti, nivo X od 9",
  krajevi skale (`1 - potpuno samostalna`), napomena uz procenu, i tri poruke o
  greškama (429, 529, nema veze sa Anthropic-om).
- `src/data/carePlan.sr.js` - tekst plana koji forma napravi na kraju.
- `src/data/prompts.js`, `src/components/PlanContents.jsx` - po jedna rečenica.
- `src/styles/app.css` - nabrajanje u „Usput ste rekli" imalo je dugu crtu kao
  znak za stavku, sada kratku.

Ostatak aplikacije (Dashboard, Plans, Settings) nije diran.

## Stranica za pregled: silueta umesto pet crtica

`public/review/understanding-silhouette.html` - predlog da meru „koliko vas
razumem" na vrhu AI forme nosi silueta starije žene sa štapom umesto pet
crtica. Četiri varijante sa klizačem: punjenje odozdo narandžastom, pravi blur
(u SVG-u, ne CSS), dupla silueta i tačkice. Ništa od ovoga još nije u
aplikaciji, stranica je samo za dogovor.

## Chat kit 0.59.0 — dve zaobilaznice manje

U kitu su sređene obe stvari koje sam prijavio, pa su naši workaround-ovi
izbačeni.

**Visina.** Kit je svojoj strani davao `height: 100dvh`, a kod nas je chat
kartica uvučena 12px gore i dole — strana je bila 24px viša i `overflow:
hidden` je sekao dno, tj. docked composer. Sada postoji prop:

```jsx
<ChatExperience fill="container" … />
```

Override `.nana-chat [class*='ick-page-']` iz `app.css` je obrisan. Mereno:
kartica 12→2062, strana kita 12→2062, composer se završava 24px iznad dna.

**Redosled rečenice i kartica.** Kit je do sada crtao sve delove *iznad* proze,
pa je rečenica „Predlažem ovu izmenu:" završavala ispod same izmene. Zbog toga
smo rečenicu slali kao custom deo `lead` i prozu uopšte nismo koristili. Sada
`useChatTurns` svakom delu upiše koliko je proze stiglo kad se pojavio, pa je
redosled na ekranu onaj kojim je poslato.

Zato je u `src/components/KitAssistant.jsx` ostalo samo `yield r.said`, a tip
dela `lead` i `.nana-chat-lead` u `app.css` su obrisani.

Uz to smo dobili nazad ono što visi o prozi: **markdown i red sa akcijama
(kopiraj / ponovo / palac gore-dole) sada postoje i na odgovorima sa
karticama** — ranije su ih imali samo odgovori bez kartica, jer taj red kit
crta unutar proze.

Provereno uživo: poruka → rečenica → kartica „Sačuvano u planu" → kartica sa
predloženim izmenama → red sa akcijama. Tim redom.

## X u dijalogu za pretplatu izgleda kao svaki drugi X

`.modal-close` je imao `width: auto`, pa se dugme skupljalo oko ikonice i X je
ostajao go, dok je isti X u svakom drugom panelu kvadratno dugme (`.ci-btn`,
28x28, sa podlogom i senkom). Uklonjen je `width: auto`, pozicija gore desno
ostaje. Tiče se oba stanja `PaywallModal`-a: i poruke negovateljici i „Ceo plan
nege".

Uz to, u `PlanEditor`-u je ostala jedna duga crta u uvodnoj rečenici, sada zarez.

## Ispravka: X je bio razvučen celom širinom

`.modal > *` razvlači svako direktno dete dijaloga, pa je `width: auto` na
`.modal-close` bio protivotrov za to, samo je uz njega dugme padalo na širinu
same ikonice. Sada stoji `width: 28px`, koliko i `.ci-btn`: ni razvučeno, ni
golo.

## Poruka negovateljici više nije prepopunjena

Polje „Poruka za negovateljicu" otvaralo se sa gotovim tekstom sastavljenim iz
plana („Tražimo negovateljicu, plan nege je u prilogu…"). Sada je prazno, sa
postojećim placeholder-om „Recite joj ukratko šta vam treba i kada.". Dugme
„Pošalji" je i dalje neaktivno dok se nešto ne napiše.

`draft` prop je izbačen iz `PaywallModal`-a i iz `App.jsx`.
